services:
  db:
    image: 'mysql:9.0'
    environment:
      - 'MYSQL_DATABASE=expense_db'
      - 'MYSQL_USER=nyanminhtet'
      - 'MYSQL_PASSWORD=r00tme'
      - 'MYSQL_ALLOW_EMPTY_PASSWORD=true'
    ports:
      - '3306:3306'